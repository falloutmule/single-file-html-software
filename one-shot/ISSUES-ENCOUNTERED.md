# One-Shot Factory Issues Encountered

| ID | Phase | Status | Severity | Observed / expected | Evidence and resolution | Remaining risk |
| --- | --- | --- | --- | --- | --- | --- |
| OSF-001 | Reconnaissance | RESOLVED | medium | The supplied workspace contained SNC, not a local SFHS checkout; the requested repository was available at `falloutmule/single-file-html-software`. | Cloned current `main` locally and created `codex/sfhs-one-shot-factory` from `697a9d8922389e517184d6376c4e382985d4ed58`. | Remote may advance before review; final report records exact base and local commit. |
| OSF-002 | Case study | WORKAROUND | low | The named Cat Paw source/evidence ZIPs and report were not present in the workspace or current SFHS repository. | Case study retains only handoff-supported, explicitly `REPORTED` facts and no invented binary evidence. | Add exact archive hashes/excerpts later if supplied. |
| OSF-003 | Verification | RESOLVED | low | `pnpm` attempted a noninteractive workspace install after a temporary generated example was added. | Used `CI=true pnpm install --no-frozen-lockfile`, packed and exactly verified the generated project, then removed the temporary project and restored the lockfile's pre-existing importer entry. | New initialized examples require the normal documented workspace install before package resolution. |
| OSF-004 | Verification | RESOLVED | low | Normal `pnpm` commands normalized an existing lockfile importer for an absent historical example. | Restored the unchanged baseline importer after each generated-tool run; the committed lockfile changes only add `@sfhs/one-shot`. | Future repository cleanup can remove the stale importer under a separate card. |
| OSF-005 | PR review | RESOLVED | medium | `one-shot init` could leave an incomplete target project if a write failed after the template copy. | Initialization now prepares a uniquely named sibling directory and renames it only after the packet is complete; failures remove the staging directory. | A process crash during the final filesystem rename remains subject to host filesystem guarantees. |
| OSF-006 | PR review | RESOLVED | high | Audit accepted packet statuses without checking all required machine facts, and a canonical report was not bound to the verifier's exact artifact identity. | Packet validation now requires schema-specific facts. CLI audit passes the actual verifier path, SHA-256, and build ID and rejects mismatches. Regression tests cover malformed packets and mismatched canonical claims. | Verification still depends on the existing SFHS verifier being executable in the target environment. |
| OSF-007 | PR review | RESOLVED | medium | The generated source kit listed files but did not retain the manifest's resolved priority, lane, and supersession authority metadata. | The kit now embeds the complete resolved `sourcePack` authority data and tests assert its presence. | External agents must still read sources in the manifest order. |
| OSF-008 | Merge review | RESOLVED | medium | The external kit included only two of seven project-packet templates and omitted the available packet schemas. | The manifest now includes every packet template and every maintained schema; CLI assertions require representative omitted-before sources. | Project-specific content and evidence remain intentionally outside the generic kit. |
| OSF-009 | OSF-BUILD-001 | RESOLVED | high | A default shell runtime could be mistaken for the only installed Node runtime. | Protocol v2 records bounded PATH, explicit, and process runtime probes separately before selecting a mode. | Tool-managed discovery remains bounded by explicit environment and path probes. |
| OSF-010 | OSF-BUILD-001 | DEFERRED | high | A portable candidate compiler must reuse the current `esbuild`-backed SFHS builder without becoming a separately maintained builder. | The v2 collector reports candidate-runtime availability explicitly; candidate compilation remains a pinned runtime-kit boundary pending its platform-bundling proof. | Environments without that runtime select SOURCE_ONLY_MODE. |
| OSF-011 | OSF-BUILD-001 | RESOLVED | medium | Completion could otherwise remain apparently valid after its source identity changed. | Run-state validation hashes the declared source and every retained record; completion fails stale identities and records a revision-bound completion JSON. | A project must update run-state intentionally after an authorized source change. |
| OSF-012 | Independent review | RESOLVED | high | An un-described runtime ZIP could be treated as a candidate compiler, and preflight could create records in a legacy project. | Candidate mode now requires a readable matching descriptor with an explicitly `AVAILABLE` compiler; preflight first validates a v2 run state and otherwise writes nothing. | OSF-010 remains the only path to an actual portable compiler. |
